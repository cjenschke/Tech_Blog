<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>{{post.title}}</title>
        <link rel="stylesheet" href="/public/css/style.css">
    </head>

    <body>
        <header>
            <h1>{{post.title}}</h1>
            <p>Author: {{post.User.username}}</p>
        </header>

        <section>
            <article>
                <p>{{post.content}}</p>
            </article>
        </section>

        <section>
            <h2>Comments</h2>
            {{#each post.Comments}}
            <div class="comments">
                <p>{{this.text}}</p>
                <p>Comment by:{{this.User.username}}</p>
            </div>
            {{/each}}
        </section>
        
       <script src="/pulbic/js/script.js"></script>
    </body>
</html>